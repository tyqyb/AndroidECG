# Bug&&DeBug

## 一、Bug描述

1.~~当打开软件启动Splash页面进入主页面后，按返回退出至桌面还会返回到Splash页面，并且终止在该页面，返回键无效，只能通过杀后台进程退出。~~

2.数据经过python滤波后图像明显发生了变形，与正常绘制的图像差异很大，可能原因是数据源本身是已经滤波完的，而不是直接采集的信号。

3.绘制完图像闪退至主页面，不稳定。

4.关闭蓝牙搜索按钮后再打开搜索不到蓝牙，估计是没有进行初始化的原因，需要根据其他蓝牙软件重写这部分逻辑。

5.数据量大时绘制完闪退，例如3s的数据循环三次。

## 二、DeBug

1.20240701解决该bug，重写页面跳转逻辑，对应res->anim->fade_in与fade_out两个函数。